# Core (required)
streamlit
PyMuPDF
requests
edge-tts
numpy>=2.0
soundfile
kokoro>=0.9.4
beautifulsoup4
pydub

# Optional: GPU acceleration for Kokoro / XTTS (install from PyTorch CUDA index)
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
# torch
# torchaudio

# Optional: XTTS voice cloning (imports as `TTS` from the coqui-tts package)
#   pip install "coqui-tts[codec]" huggingface_hub "transformers>=4.33.0,<5.0"
# coqui-tts[codec]
# huggingface_hub
# transformers>=4.33.0,<5.0

# Optional: Facebook MMS-TTS Italian (VITS via transformers)
#   pip install transformers torch torchaudio
#   # or: pip install ".[mms]"
# First run downloads facebook/mms-tts-ita from Hugging Face.

